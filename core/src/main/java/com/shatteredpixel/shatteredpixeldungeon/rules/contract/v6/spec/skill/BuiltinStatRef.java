package com.shatteredpixel.shatteredpixeldungeon.rules.contract.v6.spec.skill;

/** Stable, engine-owned values. They are never resource declarations. */
public enum BuiltinStatRef {
	HP_CURRENT, HP_MAX, HP_MISSING, HP_PERCENT, BARRIER, TEMPORARY_HP,
	HERO_LEVEL, DAMAGE_TAKEN_CURRENT_EVENT, DISTANCE_CURRENT_EVENT
}
